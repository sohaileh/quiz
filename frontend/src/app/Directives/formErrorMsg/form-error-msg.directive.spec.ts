import { FormErrorMsgDirective } from './form-error-msg.directive';

describe('FormErrorMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new FormErrorMsgDirective();
    expect(directive).toBeTruthy();
  });
});
